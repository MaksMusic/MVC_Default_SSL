# Профиль docker (docker-compose.deploy.yml задаёт SPRING_PROFILES_ACTIVE=docker).
# Упрощаем health, чтобы проверка в контейнере не уходила в DOWN из-за лишних индикаторов.
management.health.diskspace.enabled=false
